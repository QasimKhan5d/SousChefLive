id: session_timeout
name: "Session timeout — server enforces max session age"
description: >
  Session exceeds the configured maximum age; server closes the
  WebSocket with code 1000 and cleans up session resources.
tier: [integration, deploy]
judge_critical: false

setup:
  generation_config:
    response_modalities: [AUDIO]

override_env:
  SESSION_MAX_AGE: "2"

expected:
  events:
    - session_timeout
    - session_cleanup
